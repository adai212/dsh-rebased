# dsh-rebased

This workspace contains two top-level areas:

- `dsh-rebased/`: implementation target for the DSH Git plugin.
- `rebased/`: read-only JetBrains Rebased / IntelliJ reference checkout.

Do not modify `rebased/`. Project rules are documented in `AGENTS.md`.

The implementation roadmap is in `dsh-rebased/ROADMAP.md`.
